import { Navbar } from '@/components/site/Navbar'
import { Footer } from '@/components/site/Footer'
import { api, fmtDate } from '@/lib/api'
export default async function MagazinesPage(){ let items:any[]=[]; try{items=(await api.magazines()).data||[]}catch{} return <div className="min-h-screen bg-[#0B0F12] text-white"><Navbar/><main className="max-w-7xl mx-auto px-6 py-12"><h1 className="text-5xl font-bold">Magazines</h1><p className="text-white/70 mt-4">Read recent ministry magazines, reflections, and campus fellowship publications.</p>{items.length===0?<p className="mt-8 rounded-3xl bg-white/5 border border-white/10 p-8 text-white/60">No magazine issues published yet.</p>:<div className="mt-8 grid gap-6 md:grid-cols-2 lg:grid-cols-3">{items.map((m:any)=><article key={m.id} className="rounded-3xl bg-white/5 border border-white/10 overflow-hidden">{m.cover_url?<img src={m.cover_url} alt={m.title} className="w-full h-72 object-cover" loading="lazy"/>:<div className="h-72 flex items-center justify-center bg-white/10 text-white/50">Magazine cover coming soon</div>}<div className="p-6"><h2 className="text-2xl font-semibold">{m.title}</h2><p className="text-white/70 mt-2">{m.description}</p><p className="text-white/50 mt-4">{fmtDate(m.published_at || m.created_at)}</p>{m.file_url&&<a className="inline-block mt-4 px-4 py-2 rounded-full bg-emerald-500 text-black" href={m.file_url}>Open issue</a>}</div></article>)}</div>}</main><Footer/></div>}
