import { Navbar } from '@/components/site/Navbar'
import { Footer } from '@/components/site/Footer'
import { api } from '@/lib/api'
export default async function ProgramsPage(){ let items:any[]=[]; try{items=(await api.programs()).data||[]}catch{} return <div className="min-h-screen bg-[#0B0F12] text-white"><Navbar/><main className="max-w-7xl mx-auto px-6 py-12"><h1 className="text-5xl font-bold">Programs</h1><p className="text-white/70 mt-4">Admin-managed ministry programs, departments, and campus activities.</p>{items.length===0?<p className="mt-8 rounded-3xl bg-white/5 border border-white/10 p-8 text-white/60">No programs published yet.</p>:<div className="mt-8 grid gap-6 md:grid-cols-2 lg:grid-cols-3">{items.map((p:any)=><article key={p.id} className="rounded-3xl bg-white/5 border border-white/10 p-6"><p className="text-emerald-300">{p.category}</p><h2 className="text-2xl font-semibold mt-2">{p.title}</h2><p className="text-white/70 mt-3">{p.description}</p></article>)}</div>}</main><Footer/></div>}
