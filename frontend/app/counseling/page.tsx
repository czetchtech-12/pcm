'use client'
import { useState } from 'react'
import { Navbar } from '@/components/site/Navbar'
import { Footer } from '@/components/site/Footer'
import { api } from '@/lib/api'
export default function CounselingPage(){const [msg,setMsg]=useState(''); const [err,setErr]=useState(''); async function submit(e:any){e.preventDefault(); const f=new FormData(e.currentTarget); setErr(''); try{await api.counselingRequest({name:f.get('name'),email:f.get('email'),phone:f.get('phone'),service_type:f.get('service_type'),message:f.get('message')}); setMsg('Counseling request received. A trusted ministry leader will follow up.'); e.currentTarget.reset()}catch(ex:any){setErr(ex.message)}} return <div className="min-h-screen bg-[#0B0F12] text-white"><Navbar/><main className="max-w-5xl mx-auto px-6 py-12"><h1 className="text-5xl font-bold">Counseling & Pastoral Care</h1><div className="mt-6 grid gap-4 md:grid-cols-3"><div className="p-5 rounded-2xl bg-white/5 border border-white/10"><h3 className="font-bold">Confidentiality</h3><p className="text-white/70 mt-2">Requests are only visible to authorized leaders.</p></div><div className="p-5 rounded-2xl bg-white/5 border border-white/10"><h3 className="font-bold">Emergency</h3><p className="text-white/70 mt-2">For urgent danger call 0776240927 or local emergency support.</p></div><div className="p-5 rounded-2xl bg-white/5 border border-white/10"><h3 className="font-bold">Consent</h3><p className="text-white/70 mt-2">Real counselor names should only be displayed after consent.</p></div></div><form onSubmit={submit} className="mt-8 rounded-3xl bg-white/5 border border-white/10 p-6"><input required name="name" placeholder="Full name" className="w-full p-3 rounded-xl bg-black/40 border border-white/10"/><input required type="email" name="email" placeholder="Email" className="mt-3 w-full p-3 rounded-xl bg-black/40 border border-white/10"/><input name="phone" placeholder="Phone" className="mt-3 w-full p-3 rounded-xl bg-black/40 border border-white/10"/><select name="service_type" className="mt-3 w-full p-3 rounded-xl bg-black/40 border border-white/10"><option value="personal">Personal</option><option value="relationship">Relationship</option><option value="academic">Academic</option><option value="crisis">Crisis</option></select><textarea required rows={6} name="message" placeholder="Tell us what kind of support you need" className="mt-3 w-full p-3 rounded-xl bg-black/40 border border-white/10"/><button className="mt-4 w-full p-3 rounded-xl bg-emerald-500 text-black font-semibold">Request counseling</button>{msg&&<p className="mt-3 text-emerald-300">{msg}</p>}{err&&<p className="mt-3 text-red-300">{err}</p>}</form></main><Footer/></div>}
